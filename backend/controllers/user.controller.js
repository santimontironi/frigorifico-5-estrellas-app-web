class UserController {
  // Gestión de la cuenta del usuario (editar perfil, cambiar contraseña, etc.)
  // Pendiente de implementar.
}

const userController = new UserController()
export default userController
